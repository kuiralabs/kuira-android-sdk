# SDK Roadmap — internal dev tracking

**Private.** This is the detailed engineering backlog for the Kuira Android SDK —
the source the public `kuira-sdk-android/docs/roadmap.md` is distilled from. It
keeps the full context the public page strips: internal task IDs, dated status
notes, the exact example-app workarounds, source paths, and links into this
(private) monorepo. Do not copy rows verbatim into the public docs — promote
only the forward-looking capability, with no IDs/dates/paths/private links.

Items are added as real dApps (BBoard, Midnight Kicks, the Kuira Starter,
CipherDefense) hit the same friction. Most wait until a second consumer hits the
same gap. Each row records the example-app workaround in use today.

| # | Wish | Why it'd help | Today's workaround |
|---|------|---------------|--------------------|
| 1 | `awaitIndexerSynced(blockHeight)` primitive | Replace fixed `delay(5000)` waits between deploy → first call and between sequential tx. Reduces total match time and makes flow deterministic. | Hard-coded 3–8s sleeps in MatchManager (INDEXER_SETTLE_MS, POST_JOIN_SETTLE_MS). |
| 2 | `MidnightContract.stateFlow(): Flow<ContractState>` | StatePoller becomes a one-liner. Lets UI react to opponent actions without polling boilerplate. | Caller writes a polling loop against the indexer GraphQL. |
| 3 | Retry policy on `contract.call(...)` | "Indexer says contract not found, wait + retry" is a near-universal pattern after deploy. Bake retry-with-backoff into the call surface. | MatchManager.aiJoin loops 10× by hand, matching on `"not found"` substring. |
| 4 | ✅ DONE — Serializable state snapshot for BlockStore backup | `MatchStore.snapshotBytes()` / `restoreFromBytes()` round-trip the active-match set as a versioned blob; `MatchStoreBackupProvider` (bound as the SDK's `AppDataBackupProvider`) packs it into `SigilBackup.appMetadata`. Reinstall → sigil restore repopulates unfinished matches. Completed games pruned on `Resolved`. Long-term archive tier is #30. | (was: manual serialization in the app layer) |
| 5 | First-class `Player` / `Identity` abstraction | The two-player witness pattern (P1/P2 secret key, swap on each call) is recurring. A `Player` with `secretKey` + `coinPublicKey` + witness-registration helper would shrink each call site. | MatchManager passes raw `ByteArray` secrets through every helper. |
| 6 | Auto force-resync of dust around contract calls | Every tx after a deploy/tx needs `wallet.forceResyncDust()` to see new UTXO state. Should happen inside `contract.call` when needed. | Manual `forceResyncDust()` between every tx in MatchManager. |
| 7 | Deadline / timeout helper | `BigInteger.valueOf(System.currentTimeMillis()/1000 + N)` for unix-second deadlines is everywhere a circuit takes a deadline. | Computed by hand in MatchManager.aiJoin. |
| 8 | "Test-mode seed" path | The shared test seed (`TEST_SEED` in KicksActivity) should be a single SDK opt-in, not a literal in every example app. | Hex literal in KicksActivity. |
| 9a | ✅ DONE — Lossless ledger reads via SDK (`MidnightContract.ledger()`, landed 2026-05-20) | Cell-hex parsing was lossy (`Vector<N, Uint<8>>` with zero elements lost positional info). Fix: `MidnightContract.ledger()` spins up QuickJs, invokes the contract's generated `ledger(state)`, marshals typed values via `core/compact-engine/.../MidnightLedger.kt`. Typed accessors (`getUint8/64`, `getBoolean`, `getBytes`, `getVectorUint8`, `*OrNull`) with range/shape validation; `MissingLedgerFieldException` / `WrongLedgerFieldTypeException`. Kicks `ContractStateSnapshot` + BBoard `BBoardRepository` migrated, dropped bespoke parsers. | Lossy cell-hex parsing in each dApp. |
| 9b | Typed `Ledger<T>` codegen from `.compact` | With 9a, runtime decoding is lossless. Remaining gap is compile-time type safety: `ledger.getUint8("p1Score")` vs `ledger.p1Score`. Gradle plugin reads compiled JS `ledger()` descriptor, emits typed Kotlin data class per contract. Defer until a second consumer (CipherDefense) needs it. | String-keyed accessors with runtime field-name validation. |
| 10 | Public block subscription | `MidnightSdk.indexerClient` is private; app code can't reach `subscribeToBlocks()`. Expose the indexer or a thin `MidnightContract.stateFlow()`. | `StatePoller` falls back to 3s polling. |
| 11 | Kuira Gradle plugin for contract artifact sync | Copying compiled `.compact` outputs into assets (`<alias>-contract.js` + `keys/*`) is the same shape per dApp. `com.midnight.kuira.contract` plugin: declare `kuiraContract { source = "contract/src/managed/penalty" }`, get sync-before-`mergeAssets`. (Shipped in alpha02.) | `app/build.gradle.kts` hand-rolled `syncContractAssets` Copy task. |
| 12 | Typed witness factories for non-scalar types | `WitnessResult(null, ByteArray)` works for scalars; `Vector<N,T>` requires hand-assembling the runtime serialization. Expose `vectorWitness(length=5){...}`, `boolWitness{...}`. | MatchManager packs Vector witnesses by hand. |
| 13 | Compactc version pinning | compactc `--runtime-version` must match `@midnight-ntwrk/compact-runtime`; today it's a runtime error after compile. `.compact-version` file or auto-pin from package.json. (kuiraDoctor now cross-checks the pin.) | Kicks required compactc 0.30.0 (runtime 0.15.0) while 0.31.0 emits 0.16.0 bytecode; invoke a pinned compactc or `compact update`. |
| 14 | 🔴 CRITICAL — Session auto-lock | Decrypted seed lives in the process-wide `MidnightSdk` (owned by `MidnightSdkProvider`, `sdk:wallet-runtime`, 2026-05-24). After one biometric, every value-bearing call runs with zero re-auth (SDK caching bypasses Keystore's 30s window). Need idle timeout, background timeout, screen-lock observer, manual "Lock now". Unblocked: provider owns the cached SDK + `close()`. | `close()` seam exists; no caller invokes it. |
| 15 | 🔴 CRITICAL — Sigil UX safety | `SigilStatusPanel`: (a) on `restoreSeed` failure, `ErrorBody` renders a forge button → `persistSigil` overwrites the existing sigil (one wrong tap = sigil gone); (b) no in-app sign-out from `Forged`. Fixes: biometric-gated `signOut()`; danger-zone sign-out + confirm; restore-fail-from-Forged returns to Forged (never forge); forge button only from None/BackupAvailable; data-layer guard `persistSigil(replace=true)`; wire sign-in-with-existing-passkey. Multi-sigil deferred. | Force-quit + restart to reload prefs, or accept visual deletion. |
| 16 | Resume-aware multi-step protocol orchestrator | `MatchManager` is ~600 lines, ~90% generic. Three 2026-05-20 bugs all reduced to "code assumes step 1, state is at step N." `Protocol<S>` DSL: each step declares precondition on a typed `MidnightLedger`, the action, an "already done?" predicate; `protocol.runFrom(contract.ledger())` auto-skips landed steps. Folds in #1 + #6. | Per-dApp orchestrator + `resumePlayAsP*` + precondition catches. |
| 17 | `MidnightContract.callIdempotent(circuit, args, isDoneOnLedger)` | Every action needed a "contract already reflects this transition" catch. Ship `callIdempotent(..., isDoneOnLedger = { it.getUint8("phase") > PHASE_WAITING })` — SDK reads ledger first, returns synthetically if done. | Hand-rolled catch + substring-match in `joinAsP2`, `awaitOpponentJoin`, etc. |
| 18 | `WitnessStore` — encrypted multi-slot witness persistence | `MatchStore` is ~250 lines/dApp (`EncryptedSharedPreferences`, role rehydrate, ByteArray content-equality, Block Store wiring). Ship `WitnessStore(scope)` keyed by `(contractAddress, slotName)` with PRF-encrypted persistence + Block Store backup. Composes with #16. | Per-dApp `MatchStore` + `MatchStoreBackupProvider` + `rehydrateLocalIdentity`. |
| 19 | `MidnightContract.readOnly(...)` factory | `coinPublicKey` made nullable 2026-05-20 for polling contracts, but read-only still needs cargo. A `readOnly(config){...}` returning `MidnightContractReadOnly` (ledger + stateFlow, no call/deploy) makes misuse compile-time impossible. | `create{...}` with cpk-null sentinel + `requireWriteCapable()` runtime check. |
| 20 | `MidnightContract.observeLedger(): Flow<MidnightLedger>` | Sibling of #10; push-based flow over `subscribeToBlocks` drops the per-dApp 3s poller, cuts latency to ≤1 block, composes with #16. | `StatePoller` 3s loop. |
| 21 | `compact-engine-testing` artifact — `FakeMidnightContract` | Every dApp writes the same state-machine test scaffolding (`TestableMatchManager`'s `executeDeploy`/`executeJoinMatch`/`readResumeSnapshot`). Publish `FakeMidnightContract` (canned `MidnightLedger` snapshots, deferred-await knobs, per-circuit counters). | Hand-rolled `internal open suspend fun` seams + `TestableMatchManager` subclass per dApp. |
| 22 | 🔴 CRITICAL — Open-SDK permissionless integration (drop default `PasskeyConfig`) | `core:identity:IdentityModule.providePasskeyConfig()` hardcodes `rpId = "nel349.github.io"` as the default Hilt binding → misconfigured hosts silently land on the maintainer domain + need a PR to a maintainer-hosted assetlinks.json. Remove the default (fail fast); each example ships its own `*IdentityModule`; document the 4-step recipe. | Default `PasskeyConfig` in `IdentityModule` → `nel349.github.io`. |
| 23 | 🔴 VITAL UX — Single-biometric sigil forge (PRF-on-create) | Sign-in is already single-biometric (`SigilSession` multi-salt PRF). Forge shows two prompts (`createPasskey` then `deriveSigilDid`). Fix: WebAuthn PRF extension on `create` — pass `prf.eval.first = SIGIL_SALT` into registration, parse `clientExtensionResults.prf.results.first`; fall back to two-prompt for older authenticators. GPM supports it since Chrome 132 / GMS ~24.x. | Two biometric prompts on first forge. |
| 24 | 🔴 CRITICAL — Sovereign recovery-phrase export | A Sigil wallet's only recovery is re-deriving from the passkey (rides Google/Apple sync). PRF output is valid BIP-39 entropy; `SeedDeriver` already transiently makes a 24-word mnemonic via `BIP39.entropyToMnemonic`. Promote to a biometric-gated `revealRecoveryPhrase(activity)` (wipeable holder, zero in `finally`), wire `RecoveryPhraseWireframe` into Settings. `FLAG_SECURE`, no clipboard, never logged. Must read the live funding seed. | Import-from-phrase works; export is a no-op (`RecoveryPhraseWireframe` dev-portal only). |
| 27 | 🟡 Delegated access keys — scoped, time-bounded authorization | `KeyAuthorization` (Magic + rootPubkey + accessPubkey + scope + expiry, ECDSA-signed) stays in `core:identity` awaiting its real consumer: authorizing a SEPARATE non-sibling key (remote agent, third-party dApp grant, multi-sig). Needs approve-delegation UX, revocation, DID-based verification (sign with the Ed25519 sigil key, not ). | Demonstrator removed in post-A2 cleanup; no active consumer. |
| 28 | 🟡 Choose which identity/account backs a sigil at forge time | Sigil is PRF-derived from a GPM passkey; WebAuthn hides the account, so two devices on the same account collide to one sigil (bit hard during PvP testing). (i) debug-only per-device identity seam; (ii) product-level identity selection (multi-sigil, extends #15). Can't be solved with an in-app account picker — that lives in GPM/system sheet. | Switch GPM account in Android Settings before forging, or separate accounts per device. |
| 29 | 🟡 Rematch re-pairing | REMATCH auto-deploys a fresh contract (P1) / routes to join (P2) with no bridge. (i) replayable `penalty.compact` (`resetMatch` / N-match series); (ii) linked contracts (`prevMatch` ref); (iii) re-pair channel (new address rides back over relay/deep-link). | REMATCH → manual QR/link share + blank Join screen. |
| 30 | 🟡 Two-tier persistence — Block Store (active) + Drive `appDataFolder` (archive) | Block Store fits the critical recovery set (seed + active-match witnesses, #4), not archival data. (a) Tier-2 archive on Drive `appDataFolder`, seed-key encrypted; (b) shrink Tier-1 by deriving witnesses from seed + contract address over an HD path → Block Store holds only active addresses (or nothing if the indexer can enumerate participant contracts — the discovery gap). Transport de-risked 2026-06-02 by `DustCloudBackup` / `DriveBackupStorage` / `DriveAuthManager`. | Active matches via Block Store (#4); no archive tier; Tier-1 stores full random witnesses per match. |
| 31 | 🟡 Silent app-state backup + lifecycle auto-trigger | Cloud backup is manual (sigil-panel Backup → `authenticateWithPrf(BACKUP_SALT)` biometric). Post-PRF the blob carries only app metadata (seed is PRF-reconstructed), so the per-backup biometric is overkill. Offer a no-prompt backup deriving the `appMetadata` key from the cached session; app fires it on `ProcessLifecycleOwner` `ON_STOP` when the snapshot diffs. Must run in-process (not WorkManager). Interacts with #14. | Manual biometric-gated tap; no auto-capture on leave. |
| 32 | 🟡 Upstream `DustLocalState` Merkle-root accessors to `midnightntwrk/midnight-ledger` | `kuira-crypto-ffi` calls `commitment_root()`/`generation_root()` in 18 places (`balance_ffi.rs`, `dust_ffi.rs`) — a fork-only addition in `nel349/midnight-ledger` (nested as a submodule). PR them upstream as `pub fn`, then drop the submodule + switch the `Cargo.toml` path deps to upstream git-deps. | Submodule `nel349/midnight-ledger`; 8 `Cargo.toml` path deps; diff lives on the fork. |
| 33 | Contract proving-key DX — `ensureContractKeys` one-call wrapper *(remaining; 2 of 3 parts shipped)* | **(a) SOLVED** by the #256 offline wallet bundle: it ships the full `bls_midnight_2p5..2p15` set, so small contract circuits (counter `2p5`) find their size with no per-contract BLS staging — the `BLS params file not found` cold-install failure is gone (tally no longer hand-ships `2p5`). **(b) DONE:** `installCircuitKeysFromAssets` (content-aware — apk-stamp + per-file SHA-256) is the standard contract-key installer; the ~30-line asset→cacheDir→`installCircuitKeysForProving` boilerplate is retired. **(c) REMAINING:** a single `ensureContractKeys(...)` "everything proves on first call" entry point (today each dApp still calls `installCircuitKeysFromAssets()` itself). Backlog — low value now that (a)/(b) landed. | examples call `installCircuitKeysFromAssets()` directly before first deploy/call; wallet keys provisioned via `kuiraContract { bundleWalletKeys = true }`. |
| 34 | Benchmark on-device proving latency (real devices, per circuit) | On-device/offline proving is verified, but no defensible latency number. Needs physical devices (low/mid/high), circuit-complexity tiers, Dust-leg vs circuit-leg broken out, a reproducible harness. Emulator figures (`Dust tx 0.5–0.65s`, circuit `0.00–0.10s`) must not be quoted as guarantees. | Capabilities copy omits latency; only emulator log lines exist. |
| 35 | 🟡 Cross-app dust/state sync — DEFERRED to Sigil v2 (identity-model gated) | Sharing one wallet's dust across sibling apps is an identity-model decision: apps share dust only if they share `rpId` → same passkey → same seed. Production dApps host their own domains (#22) → different wallets → moot by design. Requires committing to one Kuira-owned `rpId` (core Sigil v2, rides #15/#28). Encryptor already seed-gated/forward-compatible (`DustBackupEncryptor` + `SeedDerivedKeyDeriver.deriveDustBackupKey`). `appDataFolder` scoping (per project vs per client) undocumented; Path 1 = same-project appData (decisive 5-min test: back up app A, open app B same account → delta = project-scoped); Path 2 (guaranteed) = seed-encrypted blob in a shareable regular Drive file. Transport solved either way. | v1 ships cross-device same-app dust recovery; cross-app deferred. |
| 36 | 🟡 Dust cloud-sync optimization — fresher save-away + tip-aware delta + replay progress | (a) `backupDustToCloud` only fires inside throttled `refresh()` + manual tap → Drive checkpoint lags local → staler restores → larger delta. Upload after every successful tx/sync (debounced, hash-guarded). (b) tip-aware delta: short-circuit on `chain_getBlockHash` when the tip is unchanged (wallet-cli pattern). (c) native replay emits no progress → small deltas look frozen; emit a heartbeat. | Upload piggybacks throttled `refresh()`; delta not tip-aware; native replay reports no progress. |
| 37 | 🟡 True dust-backup disable — Drive consent revoke + appData blob delete | "Disable" today is an **opt-out gate only**: `MidnightWallet.dustBackupEnabled = false` (persisted `dust_backup_opted_out`) just stops further uploads — the Google `drive.appdata` grant and the already-stored checkpoint blob both remain. A real off should (a) `DriveAuthManager.revoke()` the OAuth grant and (b) `DustCloudBackup.delete(walletAddress)` the stored blob, so nothing of the user's lingers in Drive after they turn it off. `DriveAuthManager` currently exposes only `authorize()` / `tokenFromConsent()` — no revoke; GMS revoke is fiddly. | Opt-out flag stops new uploads; existing grant + Drive blob persist. |
| 38 | 🔮 FUTURE — Sigil offer-file swaps (zSwap) | Shielded P2P / agent-mediated swaps via Midnight **offer files**: a *proven, imbalanced* `ZswapOffer` (inputs/outputs + a public `deltas` map) bech32m-encoded to a pasteable string; two complementary offers **merge non-interactively** (sum deltas → 0, embedded proofs re-prove nothing) into one atomic tx — no escrow, **public prices + hidden traders**, and a shared-liquidity pool any app can read. Fits the Sigil thesis (identity + P2P trade + agent coordination). Substrate is already in-hand from #240: `Transaction.merge()` + Pedersen binding-commitment *summing* (used for the dust-fee merge) + bech32m. NOT applicable to native (unshielded) NIGHT sends — this is the *shielded* zSwap layer. Gated on (a) shielded/zSwap SDK maturity (have `ZswapTransferBuilder` + shielded balance tracking) and (b) the in-flight **Offer Files MIP** (`midnightntwrk/midnight-improvement-proposals` PR #50 — early; the design itself counsels "ship lean"). Chia/dexie ran the same shape for 4.5y (~985k trades) → primitive proven, traction is an ecosystem bet. Revisit after #240. | N/A — #240 ships the unshielded send; no shielded-swap surface exists yet. |
| 39 | 🟡 Reliable background receive notifications (survive lock + OS network restriction) | #264 shipped the LOCAL inbound layer — `requestAttention` "your turn" heads-ups + a `nightReceipts` new-high received-funds watcher + an outbound finalization push (all in `wallet-runtime`, `AlertNotifier`/`WalletForegroundService`). But background DELIVERY is best-effort, blocked by two layers: (1) the #14 auto-lock dropped the SDK — now mitigated by a **soft-lock** (app-switch gates the UI but keeps the SDK alive; screen-off/idle/manual still hard-wipe); (2) Android tears down the backgrounded app's TCP sockets with no foreground-service network exemption — root-caused on-device via netd `InetDiagMessage: Destroyed live tcp sockets for uids={app}` immediately preceding every `Software caused connection abort`. So alerts only fire foreground / under an op-FGS (a match) / in a retry window. Two paths to "notify anytime, even screen-off": **(a)** a kept-alive monitoring FGS (network exemption — costs a persistent notification + battery), or **(b)** an **indexer/server push** (indexer notifies the device on a received UTXO; no local wallet alive — the robust answer, but shielded needs the viewing key server-side → a privacy trade-off vs the self-custody thesis). (b) preferred long-term; ties to the PIR/indexer-privacy track. **On-device confirm (2026-06-16, emulator-5556, BBoard, `development` branch):** verified the receive-funds alert has NEVER fired — the `kuira_alerts` channel does not exist on the device, while the ongoing-sync FGS notification posts/removes correctly and the finalization channel `kuira_tx_updates` is present (has fired before). So the inbound wiring (`incomingNight`/`nightReceipts` new-high heuristic + the `WalletForegroundService.attach` observer + `AlertNotifier.postReceived`) is all in place; the only missing piece is reliable background DETECTION. Two confirmed blockers: (1) when backgrounded **and idle** the op-FGS stops (`Stop FGS timeout` + `NotificationListener … notification removed event - com.midnight.example.bboard` in logcat), the process drops to cached, sockets are torn down, and `balanceFlow()` stops updating — no receipt is observed while away; (2) the observer suppresses `if (opActive \|\| inForeground) return`, so a receipt that's only seen on reopen (`inForeground=true`) is swallowed — the alert can never fire via the reopen path. **Key code nuance:** the #235 proactive background sync is DUST-ONLY — `incomingNight` rides `balanceFlow().totalNight` (the unshielded subscription + `shieldedTracker.nightFlow`), which the proactive loop does NOT refresh, so even the brief #235 FGS windows miss NIGHT receipts. Implication for the fix: it must **detect-while-away** (refresh NIGHT/shielded under a background monitoring window so a receipt lands while `inForeground=false`, path (a); or the indexer push, path (b)) — NOT relax the suppression, which is correct (don't nag when the wallet is already open). | soft-lock keeps the SDK alive on app-switch + subscription retry reconnects when the network returns → received-funds is best-effort, foreground/op-bound. |
| 40 | 🟡 Verifiable-security story — "don't trust us, verify" (privacy-neutral spine) | From the Turnkey spike (`docs/research/turnkey-vs-kuira-analysis.md`): their strongest asset is EXTERNAL verifiability (enclave attestation). Kuira has no server enclave to attest, so make each layer independently verifiable — distributed, user-owned, no third party (arguably stronger). The privacy-NEUTRAL pillars (prove the SYSTEM, not the USER → zero anonymity cost): (a) **reproducible builds + binary transparency** — anyone rebuilds the source to the identical APK hash; publish hashes to an append-only log (Sigstore/Rekor) → "the store build = the audited source, no backdoor slipped in"; (b) **open + third-party audit** of the security core (PRF seed derivation, SeedVault wrap + wipe-after-use, Schnorr/BIP-39/BIP-32/bech32m, the JNI/FFI boundary), report published; (c) **reproducible ZK circuit builds + published verifier keys** so the proving artifacts provably match the audited Compact source — ZK is verifiable BY CONSTRUCTION (the unfair advantage Turnkey lacks: transparent chains have no equivalent); (d) **published threat model / security whitepaper**. First steps (low lift): threat-model doc + reproducible-build pipeline. Audit + reproducible-circuit pipeline are funded/eng milestones. | Security rests on "trust the device secure element + our code" — not externally checkable. |
| 41 | 🟡 Hardware-attestation self-check (the direct Turnkey analog — privacy-GATED) | On-device equivalent of "keys never leave the enclave": (a) **Android Key Attestation** on the SeedVault wrapping key (`setAttestationChallenge`) → a hardware-signed cert chain (rooted in Google's attestation root) proving the key is StrongBox/TEE-backed, non-exportable, biometric-gated — verifiable by the user/auditor, not on our word; (b) **WebAuthn authenticator attestation** for the passkey (the seed ROOT) → genuine hardware, not a software fake; (c) **Play Integrity** used DEFENSIVELY (warn on a rooted/unlocked bootloader via the verified-boot signal). CRITICAL constraint unique to a privacy wallet (Turnkey attests its OWN infra → no user-anonymity cost; these attest the USER'S device → can DE-ANONYMIZE): must be LOCAL self-checks / opt-in auditor proofs, NEVER a mandatory server-sent device beacon. Reconcile with the privacy thesis before shipping. Pairs with #40. | No hardware-backed proof surfaced; "your seed is sealed in the secure chip" is unverifiable by the user. |
| 42 | 🟡 Policy / condition engine for delegated & agent signing (extends #27) | From the Turnkey spike: their `policyEvaluation` / `consensus` / `condition` + root-quorum is a mature "scoped, multi-party authorization" model. Apply the POLICY model to #27's `KeyAuthorization` (today: scope + expiry + ECDSA-signed): conditions on tx params (recipient / amount / contract), quorum / consensus for multi-sig or agent approvals, revocation, DID-based verification (Ed25519 sigil key). Borrow the policy LANGUAGE, NOT the mechanism — Kuira's principle is a real MPC VM (MP-SPDZ family), never a TEE. Real consumer is the agent SDK / delegated remote-agent keys (rides #27). | #27 `KeyAuthorization` is scope+expiry only; no condition/quorum engine; no active consumer. |
| 43 | 🟡 Background UTXO consolidation (ideally on receive) | Dust registration registers each unregistered NIGHT UTXO in its OWN tx (~30s: build + prove + submit + sync), so registration time is **linear in UTXO count**. Observed emulator-5554 PreProd with 19 NIGHT UTXOs: `MidnightSdk: Registering dust: N NIGHT UTXO(s) not yet generating (tx 10…14)`, the "not yet generating" count dropping 10→9→8→7→6 by one per ~30s tx ≈ **~10 min**. Faucet/airdrop/repeated-receive wallets get painfully slow. Fix: proactively merge NIGHT UTXOs in the BACKGROUND (largest-first, fewest inputs, capped to `MAX_TRANSFER_INPUTS` for the time-to-dismiss ceiling) — best at receive time — so the spendable/registered set stays small and registration + sends stop paying the per-UTXO cost. Reuse the proactive background pattern (#235 dust-sync) + the existing consolidation in `MidnightSdk.sendNightInternal` (`CONSOLIDATING`/`awaitMergedCoin`). Open: trigger-on-receive vs count-threshold; interplay with key-level dust registration (#239/#168 cap the registration offer to a single UTXO). | No proactive consolidation; per-UTXO dust registration runs sequentially at register/send time. |
| 44 | 🟡 Samsung Compose-overlay frame-rate throttle during long on-device ops | A Compose overlay over the Unity GameActivity/SurfaceView (Kicks send screen / wallet pill) gets its frame clock throttled to ~16fps by Samsung during long on-device work — the send-screen Lottie freezes while the `delay()`-driven StarField keeps ticking at ~16fps. **Perfetto (RFCY71QNXXJ, Android 16, 45s):** app pinned at ~16 vsync-requests/s for the whole capture, **0 `Choreographer#doFrame`**, Main never blocked >62ms, CPU 50–100% (not saturated) → the frame clock is suspended, NOT GC/CPU/memory. **A/B vs emulator-5554:** same SDK build + dust work, ~4–5× fewer skipped frames (30–44 vs 174–196) → Samsung-specific. `appCategory="social"` (the resolved *idle*-overlay fix) ships in the merged manifest but does NOT cover the long-op/heavy-CPU case. PreProd-only because the send lingers >~4s (the suspension threshold); localnet sends finish first. **Deep-research (this session) found the standard fixes FAIL:** `requestedFrameRate` HIGH, disabling adaptive RR, a perpetual `withFrameNanos` pump, a continuously-rendering child SurfaceView, a separate translucent window. Mitigations to try: a cheap/static "Submitting…" indicator (a `delay`-driven animation survives the throttle, so it won't look frozen); shorten the op (load-once #291 already trims it); or render the send on a non-overlay (separate Activity, not over GameActivity). Separate, universal floor: the ~700ms dust-spend ZK-proof CPU freeze (#288 rayon cap is partial). | `appCategory="social"` fixes the idle overlay; the long-op throttle is unaddressed; load-once (#291) shortens the frozen window. |
| 45 | 🟢 SDK native lib — x86_64 ABI SHIPPED (alpha04); FFI-repo fold still open | **Shipped (alpha04):** `core/crypto/build.gradle.kts` now sets `abiFilters = ["arm64-v8a", "x86_64"]` and `kuira-crypto-ffi/build-android.sh` cross-compiles both targets, so the AAR ships an x86_64 `.so` and the SDK runs on x86_64 emulators (Intel Macs / most CI). Verified by build: both `.a`s compile (blst/ring/midnight-ledger cross-compile clean), CMake links each into `libkuira_crypto_ffi.so`, `file` confirms the x86-64 ELF; build-only CI gate `.github/workflows/native-abi-build.yml` fails the build if x86_64 is ever dropped or mis-copied. Runtime-on-Intel-emulator smoke is left to a dev on x86 hardware (this is an Apple-Silicon shop → arm64 emulators only). Production-irrelevant (all phones are arm64); matters for DevX/CI. Consider `armeabi-v7a` only if a 32-bit target ever matters (likely not); cost is a bigger AAR/APK per extra ABI. Also fold FFI commit `2ddda75` (lives in the native/Rust FFI source repo, not checked out here) — confirm its scope before starting. | arm64-only `.so`; x86_64 emulators can't run on-device proving — use an arm64 emulator image or a physical device. |
| 46 | 🟡 Externalize dapp-ui hardcoded English → string resources (i18n-readiness) | #259 centralized the wallet SYNC states (SyncPhase enum + SyncPhaseLabels resources), but ~140 user-facing literals remain in the dapp-ui pill — `WalletPanelViewModel` (~33), `WalletSendScreen` (~22), `WalletBalanceCompact` (~15), `BackupSection` (~14), `ContractCallProgress` (~12), `SigilPanelViewModel` (~7), … — busy/message/error/Sending/Failure text + Compose UI strings. alpha05 added more in the same scope: the restore-continuity offer dialogs (`WalletStatusPanel` — "Restore your wallet data?" / "Keep your wallet synced" + buttons) and the pill sync labels (`FloatingChipTiers` "Syncing"/"Synced"). Move them all to dapp-ui `res/values/strings.xml` so a host can localize/override the embedded pill. Honors "the SDK never emits English." Large + mechanical; do as a focused pass, starting with the ViewModel state strings. | Hardcoded English literals throughout dapp-ui; hosts can't localize the embedded pill. |
| 47 | 🟡 Shielded background receive detection via a watch-only viewing key (native carve-out) | #271 ships unshielded-only background receive. Shielded scanning (`ZswapLocalState.replayEvents`) consumes the raw 32-byte SEED (derives BOTH the spend and detect keys) and `ShieldedKeys` exposes only public keys — so there is no spend-incapable viewing key today; caching one for background scanning would mean caching spend authority outside the biometric gate (drain risk). Needs native/Rust work: (1) expose the encryption secret key (viewing key) separately from the seed; (2) a scan-only `ZswapLocalState` path that detects incoming notes using only the enc-secret + coin-public key (no coin secret key → physically cannot spend or compute nullifiers); (3) persist that watch-only viewing key outside the biometric gate (a defined relaxation: read-only shielded visibility at rest, never spend) and feed it to the #271 worker. Aligns with the Phase-9 PIR / privacy-preserving-indexer direction. | Shielded receipts aren't detected in the background — unshielded-only via #271. |
| 48 | 🟡 Prompt locked-device receive via FCM high-priority push | The #271 WorkManager tier can't beat Doze's ~15-min maintenance-window cadence, so locked-device receipts are reliable-but-delayed. PROMPT delivery (wake the app instantly out of Doze) needs a server-side FCM high-priority data message: a backend chain-watcher for the wallet's address(es) + an FCM sender, an on-device FCM receiver that posts the existing `AlertNotifier` received notification, and registration of the (network, address) to watch. Pairs with #47 — the same push covers shielded receipts once the watch-only viewing key exists. Ties to the indexer/server-push path noted in #39. The #271 WorkManager tier stays as the offline/no-backend fallback. | WorkManager (#271) delivers locked-device receipts only on Doze's ~15-min cadence — delayed, not prompt. |
| 49 | 🟡 Unit-test the adaptive-TTL wiring seams (not just the window math) | The error-182 fix (size every intent's TTL to the chain's live `global_ttl`) is well-covered for the WINDOW MATH on both sides — Rust `dust_intent_ttl_window_secs` + `ledger_params_global_ttl_secs`, Kotlin `IntentTtl` + the `ttlSecondsOverride` builder test (15 tests). The gap is the WIRING that carries the value into native calls: the deploy/call `ttl_secs` JSON injection in `CircuitExecutor` (`assembleDeployTransaction` / `assembleTransaction`), the dust-registration loop in `MidnightSdk`, and the balancer's dust-intent CONSTRUCTION in `balance_ffi` (only the window helper is unit-tested, not the `Intent { ttl }` it feeds). A string-format or threading bug there silently drops the TTL → 182 again, but it's caught only on-device. Extract the JSON builders into pure functions and assert they carry `ttl_secs`; add a balance integration test asserting the produced tx's dust intent TTL ≤ `global_ttl`. | Wiring validated only by the on-device deploy after a native rebuild; the send path alone is unit-covered (`ttlSecondsOverride` → `intent.ttl`). |
| 50 | 🔮 FUTURE — Reduce/replace the QuickJS contract-execution engine | Contract circuits run in QuickJS (a ~1 MB C engine) only because `compactc` targets JavaScript — the compiled contract + `@midnight-ntwrk/onchain-runtime`/`compact-runtime` are JS. QuickJS is NOT a bottleneck (execution is ~ms; proving dominates ~0.8s) and NOT a Kuira choice (gated on compactc's JS target). Two tractable moves, neither a full rewrite: **(a) discipline** — keep the JS a pure, deterministic transcript producer; all chain-aware values (TTL, fees, time, policy) live native/Kotlin, never in the JS (the error-182 saga traced partly to the JS stamping TTL via `Date.now()`); **(b) engine-swap spike** — QuickJS (C) → Boa (pure-Rust JS engine) to drop the C/unsafe surface + unify on Rust (modest win, real compat risk: the midnight runtime must run on Boa; zero perf gain). Full removal needs a non-JS Compact target (WASM/bytecode/Rust) — an upstream Midnight decision, analogous to Kuira already porting the LEDGER to native Rust instead of the WASM bundle. Do NOT reimplement `onchain-runtime` natively: the transcript must stay bit-exact with the node's validation (cf. the ReadMismatch + node-version-skew bug classes), so a parallel native runtime doubles the version-tracking burden the ledger FFI already carries. "Kotlin instead" isn't an option — it still needs a JS engine or a native target. | QuickJS runs the compiled contract JS + the JS onchain/compact runtimes; native Rust handles proving + ledger + tx assembly. |
| 51 | 🟡 Shared Drive OAuth client per rpId/domain — drop per-app GCP Android-client registration | `DriveAuthManager.authorize()` calls `Identity.getAuthorizationClient(context)` with **NO configured client** (verified: no `google-services.json`, no client-ID string anywhere in the SDK/example trees), so Google falls back to the implicit Android OAuth client matched by **(package + signing SHA-1)** → **every sibling app needs its own Android OAuth client hand-created in the GCP console** (BBoard `com.midnight.example.bboard`, Kicks, Starter `com.kuiralabs.starter.counter` each got one — all on the shared `~/.android/debug.keystore` SHA-1 `6C:42:…`, differing only by package). This breaks the rpId model: apps on the same domain already share the wallet/sigil with ZERO per-app setup (`assetlinks.json` on the domain), but the Drive-backup OAuth doesn't follow that boundary — a new sibling silently fails consent with `UNREGISTERED_ON_API_CONSOLE` until someone registers it, so the cross-app dust restore (the shared seed-encrypted `kuira-dust-backup.bin` in the project-scoped `appDataFolder`) never runs → PreProd genesis-replays instead of reconciling from BBoard's checkpoint. Fix: authorize through ONE shared client tied to the **domain** (a web/installed client ID passed to the `AuthorizationRequest` via a PKCE flow — likely AppAuth, a **new dependency + a change to the security-critical auth path, needs sign-off**) → register ONCE per rpId/domain; every app under it restores with no console step. Per-app registration → per-domain. Caveat: production dApps host their own domain (#22) → own rpId → own single client (still one per domain, never per app); the shared client mainly collapses the dev/example fleet onto one registration. Rides Sigil v2 cross-app convergence (#15/#28/#35) but is a **separable** auth-layer change; pairs with #37 (Drive revoke) on the same `DriveAuthManager`. | Hand-register a per-app Android OAuth client (package + debug SHA-1) in BBoard's GCP project for each new sibling app — `UNREGISTERED_ON_API_CONSOLE` until done. |
| 52 | 🟡 Full UTXO reconcile against the indexer's current set (prune ghost coins from missed spent-events / cross-app) | The unshielded UTXO cache is purely EVENT-DRIVEN: `UtxoManager` applies streamed `created`/`spent` deltas from `subscribeToUnshieldedTransactions(address, fromTransactionId=cursor)`, and the ONLY full prune is `UtxoManager.clearUtxos`, called by `ChainResetGuard` solely on a genesis-hash change. There is NO reconcile against the indexer's CURRENT unspent set, so any spent-event the app doesn't observe — backgrounded when ANOTHER app on the shared wallet spent a coin, or a subscription reconnect advanced the cursor past the removal — leaves that coin `AVAILABLE` forever (no genesis change → `ChainResetGuard` never fires). **Confirmed on emulator-5556:** Starter's `unshielded_utxos` held **27 AVAILABLE NIGHT UTXOs = 21,022 NIGHT** while `mn balance` showed **1 UTXO = 10,000**; BBoard/Kicks stayed online through the spends and read correctly. Same family as the 195/196 stale-UTXO/dust SEND-path bugs, but here it silently inflates the displayed BALANCE. Fix: on `refresh()` (and/or a periodic tick) fetch the address's current unspent set from the indexer and set-difference-prune local `AVAILABLE` rows not in it (mark-spent or delete), so the balance self-heals from missed events regardless of cause — covering the cross-app case the genesis-only `ChainResetGuard` wipe cannot. **Phasing:** the AUTO-reconcile needs a NEW indexer query (verified: no one-shot UTXO query today — only the subscription + `queryDustEvents`/`queryZswapEvents`/`queryContractState`) → **alpha05**. For **alpha04**, ship the EASY workaround instead: expose the SDK's EXISTING full-resync (`SubscriptionManager.performFullResync` + `UtxoManager.clearUtxos`, already what the chain-reset wipe calls) as a user-triggerable **"force re-sync"** affordance (pull-to-refresh / settings) + a "balance off? re-sync" UX line, so a confused user self-heals. Zero-code interim: clear app data, or reset the localnet to trip `ChainResetGuard`. | Backgrounded/cross-app missed spent-events leave ghost AVAILABLE coins → over-counted balance (Starter 21k cache vs real 10k on-chain); nothing prunes them short of a chain reset or app-data wipe. |
| 53 | 🟡 alpha05 — DUST "Register" affordance: show the unregistered-coin count + a correct tooltip | The DUST row shows a bare "Register" chip whenever the truthful per-UTXO check (`computeDustRegistered` in `MidnightWallet.kt`, `unregistered == 0`) finds ANY NIGHT UTXO not yet generating dust. With dust already present it reads like a glitch ("why register if dust is registered?"). The logic is CORRECT — each NIGHT coin must be individually registered to generate the dust that pays its own spend, so a fresh airdrop / received transfer / change-output coin legitimately needs registration — but the copy doesn't convey it. Confirmed on emulator-5554: a 12th NIGHT UTXO arrived → logcat `dust_filter_unregistered_night: 1 of 12 NIGHT UTXOs not yet generating` → bare Register chip while ~2.32 dust showed. Fix (UI-only, dapp-ui `WalletBalanceCompact`): surface the count (e.g. "Register · 1") + a correct tooltip — NIGHT *generates* dust, a coin doesn't "need" it, so accurate phrasing is "a new NIGHT coin isn't generating dust yet — register it". Verify whether an unregistered coin is genuinely unspendable (dust fungibility across UTXOs) before asserting that consequence in copy. Do NOT change the per-UTXO logic. | Bare "Register" chip; tapping it correctly registers only the unregistered UTXO(s), but the prompt looks like a bug when dust already exists. |
| 54 | 🟡 Gate the BlockStore cloud-backup on network availability (skip the Drive call when offline) | Every checkpoint store with `setShouldBackupToCloud(true)` triggers a GMS `Identity` Drive-`appdata` OAuth token fetch; on a device with no route to Google (a localnet emulator wired only via `adb reverse`, or any offline device) that fails and floods logcat on EVERY store with `getToken() NETWORK_ERROR` / `dblk: Connectivity error` / `mobstore_salt.pb.lock` IOExceptions. It's functionally harmless — the LOCAL BlockStore write succeeds and only the cloud UPLOAD fails; chain tx are unaffected (confirmed on emulator-5554: the spam interleaved with a commit that still `FINALIZED`) — but during debugging the noise reads like a transaction failure and sent us chasing a non-bug. Fix: check connectivity (or quietly catch the no-network token failure) before the Drive upload, and defer/retry the cloud sync when connectivity returns, so offline runs stay silent. | Ignore the GMS `drive.appdata` errors on an offline emulator — the local checkpoint still persists and on-chain ops are unaffected; the upload just retries when online (a real device with internet never hits it). |
| 55 | ✅ FIXED 2026-07-06 — Free native state-pool handles in the view-call READ path (readMode leak) | `MidnightContract.read()`/`CircuitExecutor.readCircuit` (18c60eb) runs a circuit and captures its result but skips `assembleTransaction`, whose try/finally `freeStateHandles` is the ONLY caller-side free — so the swapped-in `__native_stateCreate` handle + the post-circuit query-context handle leak on EVERY read into the process-global Rust `STATE_POOL` (contract_ffi.rs:89, survives QuickJS teardown). The Vault screen reads 5+2P circuits per refresh and refreshes on every ledger emission → hundreds of leaked full-contract-state entries per hour → unbounded native growth. Fix: free JS-side in the read executionTail right after the result is serialized (mirror LedgerEvaluator.kt:206's `__native_stateFree` pattern), covering both the context handle and the original on-chain handle if they differ. Test-first: add a `contract_state_pool_len()` FFI + Kotlin binding, then an instrumented leak test (N reads → pool back to baseline; write path stays baseline). Confirmed by /code-review 2026-07-06. | Restart the app periodically; keep Vault screens short-lived (each read leaks one full contract state). |
| 56 | 🟢 MOSTLY SHIPPED 2026-07-07 (batch + retry + tests; Bytes normalization still open) — View-call read hardening | Four gaps the first read() consumer (Vault) hit: (a) each read() does its OWN `fetchContractState` + fresh QuickJS boot (runtime+contract parse + initialState) — a 3-proposal Vault refresh = 11 fetches + 11 engine boots, and reads within one refresh can straddle DIFFERENT chain states; fix = `read(..., onChainStateHex)` overload (readCircuit already takes the hex) + a `readMany(circuits)` that runs all circuits in ONE engine session against ONE fetched state (consistent snapshot). (b) every consumer hand-rolls retry-until-indexed after deploy/connect (vault ViewModel 60s/2s, its e2e 90s/3s — two divergent copies) while the SDK already owns `awaitContractStateIndexed`/`pollUntilChanged`; expose a retry option on read()/deploy. (c) `JS_JSON_SAFE` near-duplicates `JS_DEEP_CONVERT` and misses struct-nested Bytes (state-serde emits plain [] arrays, not Uint8Array) → top-level Bytes = hex but nested = int array, forcing consumers to write dual-shape decoders (vault bytesHex); unify the walkers + either descriptor-aware hexing (a blind array→hex would corrupt genuine Vector<N,Uint<8>> returns) or a MidnightLedger-style typed read wrapper. (d) the read API shipped with zero unit tests (CLAUDE.md test-first) — add instrumented coverage alongside the leak test in #55. From /code-review 2026-07-06. | App-side: cache nothing, eat the 5+2P fetch/boot cost per refresh; hand-rolled retry loops; per-app dual-shape bytes decoding (vault VaultContract.bytesHex). |
| 57 | ✅ FIXED + on-device VERIFIED 2026-07-08 (captured live: `Chain reset … checkpoint at height 7140 is gone or changed; wiping shielded + dust + unshielded caches`; DataStore pin moved 7140→148 on the fresh chain) — `ChainResetGuard` couldn't detect a localnet reset (genesis is deterministic). HOTFIX: the first checkpoint lookup sent `height` as a STRING GraphQL variable into `Int!` → "expected type Int" error → `Unavailable` → the guard silently skipped (a stale on-device pin at old-chain height 7140 proved it; the celebrated post-reset zeros came from the event-driven subscription paths, NOT the guard). Fix: inline the height into the query text; regression pinned by `BlockHashLookupIntegrationTest` against the REAL indexer (RED on the string-variable transport, GREEN after) | The guard exists and is wired into all three sync entry points, but its ONLY discriminator was the GENESIS (height-0) block hash on the premise "genesis differs across a reset." **Empirically FALSE for localnet:** a full volume-wiping `mn localnet down && up` reproduced the identical genesis hash byte-for-byte (`5443d0e1…55ae3858` before and after, verified live), so a reset read as `SAME` → caches never wiped → stale dust → node error 171 on the next spend (the exact bug; the only cure was a manual app-data clear). Compounding it: `wipeOnResetEnabled` was gated on `NetworkConfig.developmentMode`, which is ALSO true on PreProd/Preview (it means "allow local proof-server HTTP") — so a more-sensitive discriminator would risk wiping a healthy remote wallet. Fix: pin a CHECKPOINT block ABOVE genesis (`ChainCheckpoint(height,hash)`) and re-look-it-up each sync via a new `IndexerClient.getBlockHashAtHeight → BlockHashLookup {Found|NotOnChain|Unavailable}`; `NotOnChain` (fresh chain shorter — proven live: old tip height → `block: null`) or a changed hash ⇒ RESET; `Unavailable` ⇒ UNKNOWN/skip (a flaky indexer never wipes). Split the wipe gate onto a new `NetworkConfig.isLocalDevChain` (UNDEPLOYED only). Test-first: `ChainResetGuardTest` now reproduces the failure at the decision level (identical genesis but `NotOnChain`/different-hash ⇒ RESET). Closes the root cause behind #52's "genesis-only ChainResetGuard wipe cannot." | Manually clear app data (or reset the localnet a second time hoping to trip it) — the guard silently no-op'd on every real localnet reset. |
| 58 | 🔵 DESIGN — Adaptive proving: route each contract proof local vs remote by COST **and** PRIVACY (full design: `docs/design/adaptive-proving.md`) | Contract circuit proving is hardcoded LOCAL (`MidnightConfig.kt:57` `proofProvider = LocalProofProvider`); `ProvingMode.REMOTE` offloads only WALLET txs (`TransactionSubmitter` :146). So heavy contract circuits can OOM on memory-limited devices — the Private Vault's `pvExecute`/`pvApprove`/`pvRevoke` (~5.2 MB keys) returned `null` (out of memory) on the e2e emulator; the public vault's light circuits never hit it. The twist that makes this a design not a switch: remote proving sends the WITNESS (private inputs) to the proof server, so a third-party server would see a private-vault proposal's recipient/amount/salts — "big → remote" naively LEAKS. Design: replace the hardcoded provider with an `AdaptiveProofProvider(local, remote?, policy)` that routes on (a) COST — prover-key size / `k` (already on device; contract-info.json lacks k, parse from the key/.bzkir header) vs the device's real `ActivityManager.MemoryInfo` budget, and (b) PRIVACY — a `ProvingPolicy` that stays local-first, only goes remote to a TRUSTED (self-hosted) server, and warns/blocks before leaking a sensitive witness to a third party. Phasing: (1) adaptive router + PER-CIRCUIT local/remote policy (cheap, no new crypto — e.g. approve→local to protect identity, execute→remote-eligible since Tier 1 discloses recipient/amount anyway) + local-first-with-fallback; needs a NEW `RemoteProofProvider` (today only `LocalProofProvider` implements the interface; remote logic lives in `core/ledger`'s `ProofServerClient`). (2) self-hosted proof-server config + `Trusted/ThirdParty` trust gate. (3) RESEARCH — circuit decomposition + commitment bridging (recursion): prove witness-knowledge LOCALLY → public commitment `C` → heavy sub-circuit remote takes only `C` (never the witness); feasible when the heavy work is expressible over `C` not raw `w` (our execute qualifies, being disclosure-heavy); far frontier = delegated/masked proving + collaborative zk-SNARKs. Open gaps enumerated in the design doc (circuit identity from an unproven tx, `k` extraction, calibration, recursion support/overhead, fallback UX, local-vs-remote determinism cross-check). | Use `ProvingMode.REMOTE` (offloads wallet txs only, and would leak contract witnesses if it applied); on a heavy contract, a low-RAM device just OOMs. E2e workaround: fund the co-signer AFTER the deploy to cut peak memory. |
| 59 | 🔵 INVESTIGATE (risk vs benefit unresolved) — Auto-retry contract calls on node error 104 (concurrent contract-state conflict) | Node `Custom error: 104` (code 1010 Invalid Transaction) rejects a contract-call submit when two devices write the SAME contract concurrently — the second tx's ZK proof/transcript binds to a contract state the first already advanced, so the node's apply/well-formed check refuses it. The tx PROVES + BALANCES fine locally; only the node rejects it, and it SELF-HEALS on a manual retry (rebuild + re-prove against fresh state). 104 is NOT in the SDK's documented custom-error set (`NodeRpcException.kt`: 115 InvalidProof / 171 OutOfDustValidityWindow / 186 EffectsCheckFailure / 195 InputNotInUtxos / 196 DustDoubleSpend), so `TransactionSubmitter`'s existing stale-dust/UTXO auto-recovery never triggers. Candidate: detect 104 → auto rebuild+re-prove+resubmit ONCE against fresh contract state (mirror the 195/196 bounded single-retry shape). **OPEN — investigate before building:** (a) confirm 104's real meaning in the midnight-node error enum (rule out version skew — ledger was coherent + skew wouldn't self-heal); (b) risk vs benefit — how often does concurrent same-contract write actually happen outside localnet multi-device testing? a blind auto-retry could mask a genuine reject or cause a double-submit; (c) repro is hard to force deterministically (needs two concurrent writers). Observed 2026-07-09 on `pvApprove` across emulator-5556/5558. Memory: `project_error104_concurrent_contract_state`. | Retry the action manually — it rebuilds + re-proves against fresh state and succeeds. Transient under concurrent multi-device contract writes on localnet's fast blocks; a single-user app rarely hits it. |

| 61 | 🔵 PLANNED (design: `docs/projects/dust-backup-restore-continuity.md`) — Dust-backup restore continuity: the backup preference travels with the wallet, first sync prefers restore over genesis | On any fresh install the dust-backup toggle state is lost (package-local prefs, `kuira_dust_backup`) and the checkpoint restore silently fails: `DustSyncManager.maybeSeedCheckpointFromCloud` → `DriveBackupStorage.fetch` → `silentTokenOrThrow` throws `DriveConsentRequiredException` (no Drive grant for this app's OAuth client yet) → swallowed → "genesis fallback" → full event replay (minutes on PreProd) even though a one-tap consent would restore in seconds; the toggle shows OFF for a wallet that has cloud backups. Plan: (1) carry wallet-level backup prefs in the seed-keyed app-state blob (Block Store, silent restore; versioned SDK envelope wrapping the host payload, legacy blobs decode host-only); (2) restore gate before the first no-checkpoint dust sync — silent Drive fetch first (same-app reinstall/new-device just works), else ONE consent prompt when prefs-say-enabled OR identity-was-restored (the cross-dApp first-run signal), decline remembered, opted-out never prompted; (3) SDK awaits a host restore gate before its first no-checkpoint sync so the proactive sync can't race into genesis; (4) toggle derives from restored prefs + consent state. Facts pinned: the dust Drive blob is a per-address BUNDLE (address embeds network → PreProd fetches PreProd — keep + test); Block Store is per-app (the flag can't cross dApps); Drive appDataFolder is per GCP project and all dApps share one project → cross-dApp fetch works after the new dApp's consent. | Re-enable the toggle manually on every fresh install and eat the genesis replay. |
| 60 | 🔵 DevX (NOT a correctness gap) — auto-rebinding contract handle to remove the per-dApp re-bind boilerplate | A `MidnightContract` handle captures its `MidnightConfig` at `create()`, and `config.getBalancer()` resolves the wallet that config was built from. The shared SDK is REPLACED — and the old instance CLOSED — on every session re-auth (`SessionLock` drops the SDK on device-lock; `MidnightSdkProvider` republishes on unlock) and on network switch; closing the old SDK tears down its `NodeRpcClient` (OkHttp `SilentSupervisor()` job → `Completed`), so a handle built BEFORE the swap fails its next balance/submit with `Parent job is Completed` (ZK proof succeeds, then `httpClient.webSocket()` can't start). **Two cases, both already handled — no correctness gap remains:** (a) BETWEEN-op (lock/unlock, then reuse a captured handle — the confirmed BBoard bug) is solved by the app-side re-bind (resolve `provider.sdk.value` LIVE per op + rebuild on instance change), shipped + on-device verified; (b) MID-op (lock DURING a contract call) is solved by the SDK ALREADY — `SessionLock.bindOperationHold` rides `sdk.operations.active` (every call registers via `MidnightConfig`'s `trackContractCall → OperationRegistry`) and `lock()` DEFERS teardown while ≥1 op is in flight (`"Lock DEFERRED — N operation(s) in flight"`). So this row is a DevX de-duplication only: a stable provider-backed `TransactionBalancer` proxy (in `wallet-runtime`, forwarding to `provider.awaitSdk().wallet`) injected as the SDK config's balancer would let handles survive swaps transparently, so new dApps don't hand-roll the re-bind. NOT urgent, NOT a correctness fix; needs the full gate if pursued. Scope caveat: the proxy covers the WRITE/balance path (where the bug manifests); the READ/fetch path staleness is theoretical (reads re-fetch, don't open the submit socket) and the app-side rebuild already covers it. | Per-dApp app-side re-bind (bboard `liveContract()`; starter/tally/vault `readHandleFor` keyed on SDK identity; kicks `requireSdk` live). Correct + verified; the only cost is boilerplate repeated per dApp. |

---

## Related (separately tracked)

- **Checkpoint consistency / `NonLinearInsertion`** — atomic `saveCheckpoint`/`loadCheckpoint` so a torn (state, cursor) pair can't be persisted or restored; self-heal after one genesis sync. Fixed in alpha02.
- **Proactive background dust-sync** — keep the dust state current ahead of need so a tx never waits on a cold sync (delta-only, energy-efficient). alpha03 target.

Promote a row to the public roadmap only as a forward-looking capability theme — strip the ID, the dates, the status notes, the workaround column, and every internal path / private-repo link.
