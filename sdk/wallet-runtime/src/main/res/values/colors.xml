<resources>
    <!-- Sync notification accent. Monochrome per the Kuira design standard (no accent hue —
         contrast/weight carry meaning; was SuccessText green). White reads as the progress fill on
         the dark notification + Live-Update chip; the runner large icon carries the brand. -->
    <color name="kuira_sync_accent">#FFFFFFFF</color>
    <!-- Milestone dots on the Live-Update progress bar — soft white ticks on the track. -->
    <color name="kuira_sync_point">#CCFFFFFF</color>
</resources>
